local function __TS__ArrayFind(arr, predicate)
    local len = #arr
    local k = 0
    while k < len do
        local elem = arr[k + 1]
        if predicate(nil, elem, k, arr) then
            return elem
        end
        k = k + 1
    end
    return nil
end
